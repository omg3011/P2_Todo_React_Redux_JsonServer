import { Card, CardContent, Typography } from "@mui/material";
import React from "react";

const CounterTodo = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Total Count: 5</Typography>
      </CardContent>
    </Card>
  );
};

export default CounterTodo;
